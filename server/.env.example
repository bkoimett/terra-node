PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/terranode
# Comma-separated for multiple origins (production + local)
CLIENT_URL=http://localhost:5173

# Production (Render single-service deploy)
# NODE_ENV=production
# SERVE_CLIENT=true
# CLIENT_URL=https://your-app.onrender.com
